{
	"name": "Admitto",

    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },

    "runArgs": ["--name", "admitto-devcontainer"],

    "features": {
		"ghcr.io/microsoft/aspire-devcontainer-feature/aspire:2": {},
		"ghcr.io/devcontainers/features/docker-in-docker:4.1.0": {},
		"ghcr.io/devcontainers/features/powershell:2.0.2": {},
		"ghcr.io/devcontainers/features/node:2.1.0": {},
		"ghcr.io/devcontainers/features/python:1": {},
		"ghcr.io/devcontainers-extra/features/uv:1": {}
	},

    "remoteUser": "vscode",

	"hostRequirements": {
		"cpus": 4,
		"memory": "16gb",
		"storage": "32gb"
	},

	// Use 'forwardPorts' to make a list of ports inside the container available locally.
	// "forwardPorts": [5000, 5001],
	// "portsAttributes": {
	//		"5001": {
	//			"protocol": "https"
	//		}
	// }

    "mounts": [
        "source=admitto-devcontainer-data,target=/home/vscode,type=volume",
        "source=${localEnv:HOME}/AGENTS.md,target=/home/vscode/AGENTS.md,type=bind,ro",
        "source=${localEnv:HOME}/AGENTS.md,target=/home/vscode/CLAUDE.md,type=bind,ro"
    ],

    "postCreateCommand": "${containerWorkspaceFolder}/.devcontainer/postCreate.sh",

	"customizations": {
		"vscode": {
			"extensions": [
                "ms-dotnettools.csdevkit",
				"GitHub.copilot-chat",
				"microsoft-aspire.aspire-vscode"
			],
			"settings": {
				"terminal.integrated.defaultProfile.linux": "zsh"
			}
		}
	}
}
