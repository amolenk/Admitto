# Specs: direct-handler-injection

This change contains no user-facing capability changes.
All modifications are to the infrastructure and DI wiring layers only.
No `openspec/specs/` files are added or modified.
